{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for mdSilo",
  "windows": ["main", "splashscreen"],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default"
  ]
}
